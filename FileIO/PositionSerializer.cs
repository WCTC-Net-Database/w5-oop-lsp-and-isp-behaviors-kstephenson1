namespace w5_assignment_ksteph.FileIO;

using w5_assignment_ksteph.DataTypes.Structs;

public class PositionSerializer
{
    // InventorySerializer contains fuctions to turn a string into List<Item> to Inventories and vice versa.
    public static Position Deserialize(string positionString)       // Converts String into Position
    {
        return new();
    }
    public static string Serialize(Position position)               // Converts Position into string
    {
        return "";
    }

}
